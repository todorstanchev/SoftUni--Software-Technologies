function printNumbersBackwards(strNum) {

    let num = Number(strNum);

    for (let index = num; index > 0; index--) {

        console.log(index);
    }
}

//printNumbersBackwards(['5']);
//printNumbersBackwards(['2']);